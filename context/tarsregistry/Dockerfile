ARG REGISTRY_URL
ARG BUILD_VERSION
FROM $REGISTRY_URL/tars.cppbase:$BUILD_VERSION
ENV ServerType=cpp
ARG BINARY
ARG TARGETOS
ARG TARGETARCH
COPY /root /
COPY /binary/${BINARY}_${TARGETOS}_${TARGETARCH} /usr/local/tars/${BINARY}/bin/${BINARY}
RUN chmod +x /bin/entrypoint.sh
